export const TOTAL_NUM_OF_QUESTIONS = 15;

// in ges, one town = 1 level (el1, el2 etc.)
// at the moment, we assume the game map is one level per town, and hopefully keep it that way
export const TOTAL_LEVELS_GES = 1;
